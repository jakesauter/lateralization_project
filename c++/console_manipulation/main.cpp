#include <iostream>
#include <string>
#include <screen.h>

using namespace std;

int main()
{
	gotoxy(5, 3);
	//getch() will get keyboard response
	//getche(); with wait for input and not wait for the enter key
	return 0;
}
